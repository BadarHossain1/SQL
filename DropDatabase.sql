create database college;
create database IF NOT EXISTS college;

DROP DATABASE IF EXISTS company; 
--It will drop the database company if it exists, without IF EXISTS it will show error because company database doesn't exist.