-- Aggregate functions take a lot of values and gives a single value. 

SELECT max(marks);
FROM STUDENT;

SELECT min(marks);
FROM STUDENT;

SELECT sum(marks);
FROM STUDENT;


SELECT avg(marks);
FROM STUDENT;

SELECT count(marks);
FROM STUDENT;

SELECT count(*);
FROM STUDENT;

